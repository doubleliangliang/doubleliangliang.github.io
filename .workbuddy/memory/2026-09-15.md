# 2026-09-15

## Hexo + Redefine 主题文档编写

- 项目 `D:\hexo-site\my-website`：Hexo 8.1.2 + hexo-theme-redefine 2.9.0。
- 关键事实：**主题通过 npm 安装**，实体在 `node_modules/hexo-theme-redefine`，`themes/` 目录为空（仅 .gitkeep）。主题配置走根目录 `_config.redefine.yml`（Hexo 的 alternate theme config 机制）。
- 生成命令实测可用：`node node_modules/hexo/bin/hexo clean|generate`（232 文件 / 5.87s）。本机 Bash 需先补 PATH：`export PATH="/c/Users/FenghuaLiangLiang/.workbuddy/binaries/node/versions/22.22.2-3:/c/Users/FenghuaLiangLiang/.workbuddy/binaries/PortableGit/versions/1.2.0/bin:/usr/bin:/bin:$PATH"`，否则缺 coreutils/node。
- 产出 `doc/` 六份中文文档：README（索引+速查+上线清单）、01 安装与目录结构、02 配置详解（16 段）、03 文章与页面（Front-matter 全表 + 9 类页面）、04 写作模块（callout/grid/tabs/folding/button）、05 插件与自定义、06 常见问题排查。内容全部依据主题源码实读（`layout/`、`scripts/modules/`、`scripts/helpers/page-helpers.js`、`source/css/layout/_modules/callout.styl`）而非二手文档。
- 遗留待办（已写进 README）：站点 `url` 仍是 example.com；`info.url` 仍是 redefine.ohevan.com；home_banner.title 仍是 "Theme Redefine"；waline serverUrl 是占位地址且 comment 已开启；`source/images/` 下缺 favicon/avatar/OG 图；仍未建 tags/categories/archives 页面。
